apiVersion: v1
    kind: Service
    metadata:
      name: devops-demo-backend
    spec:
      selector:
        app: devops-demo
      ports:
      - port: 5000
        targetPort: 5000
      type: ClusterIP
